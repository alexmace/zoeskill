<VirtualHost *:80>
    DocumentRoot /vagrant/public
#    ServerName {{server_name}}
#    SetEnv ENVIRONMENT {{app_environment}}
#    <Directory {{app_source}}>
#        AllowOverride All
#        Require all granted
        # Required for PHP to be able to handle the HTTP Basic Auth
#        CGIPassAuth on
#    </Directory>
#    <FilesMatch \.php$>
#        SetHandler proxy:unix:/var/run/php-fpm.sock.collins-middleware|fcgi://dummy
#    </FilesMatch>

#    {% if app_environment != "development" %}
#    # In the production environment, redirect HTTP connections to HTTPS
#    RewriteEngine On
#    RewriteCond %{HTTP:X-Forwarded-Proto} =http
#    RewriteRule . https://%{HTTP:Host}%{REQUEST_URI} [L,R=permanent]
#    {% endif %}

#    <Location /kibana/>
#        ProxyPass http://localhost:5601/
#        ProxyPassReverse http://localhost:5601/
#    </Location>

</VirtualHost>
